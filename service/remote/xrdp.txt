#Remote Desktop Protocol (RDP) server
sudo apt-get install xrdp

# При подключении клиентом от Windows XP имеет следующие проблемы:
* При вводе символа "d", сворачивает теукщее окно: в консоле нельзя даже ввести "cd"
* Иногда не реагирует на клавиатуру вообще
